# ============================================================
# REQUIRED — the app won't start without these.
# ============================================================

# Supabase (Project Settings → API)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Supabase service-role key. Bypasses RLS; used only by server-side
# routes (the webhook, automation engine, and the public API key
# auth path — see docs/public-api.md). Keep this secret — never
# paste it into client code.
#
# Note: the public API (/api/v1) needs no new env var. API keys are
# created in the dashboard (Settings → API keys) and stored hashed
# in the database; this service-role key is what lets the auth path
# look a presented key up without a user session.
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# WhatsApp token encryption (64 hex chars = 32 bytes, AES-256-GCM).
# Generate with:
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# Rotating this value orphans every token encrypted under the previous
# key — users have to re-save their WhatsApp settings to reconnect.
ENCRYPTION_KEY=your-64-char-hex-key-here

# Meta App Secret (Meta for Developers → App Settings → Basic).
# Verifies the HMAC-SHA256 signature on every inbound webhook POST.
# Required — without it the webhook rejects every request.
META_APP_SECRET=your-meta-app-secret

# ============================================================
# RECOMMENDED — safe defaults exist but you'll want to set these.
# ============================================================

# Canonical public URL of this deployment (scheme + host, no trailing
# slash). Used for the sitemap and OG images. Routes that need a
# self-referential URL (invite links from /api/account/invitations,
# any future email-bound link) derive the origin from the request
# itself, so this variable is *only* needed when the request-derived
# origin would be wrong — e.g. when generating links from a cron job
# or a background worker that has no incoming request.
NEXT_PUBLIC_SITE_URL=https://crm.example.com
# NEXT_PUBLIC_APP_URL — fallback para construir la URL del webhook de Telnyx
# cuando NEXT_PUBLIC_SITE_URL no está definida (src/app/api/telnyx/webhook/route.ts:102).
NEXT_PUBLIC_APP_URL=https://crm.example.com

# Origins EXTRA con permiso de CORS en /api/events y /api/track — el
# tracking del sitio web del cliente (Astro, dominio propio) llama a esos
# endpoints cross-origin y el navegador exige la cabecera.
#
# La allowlist de src/lib/cors.ts es LANDING_SITE_URL + PUBLIC_SITE_URL +
# NEXT_PUBLIC_SITE_URL. Cuando el CRM y la web viven en dominios distintos
# (crm.tudominio.com y tudominio.com), NEXT_PUBLIC_SITE_URL apunta al CRM
# y es ESTA la que mete a la web en la allowlist. Declárala siempre que
# los dominios difieran: si el único origin permitido acaba siendo el del
# CRM, la landing deja de trackear sin un solo error en los logs del
# servidor — el rechazo ocurre en el navegador del visitante.
# LANDING_SITE_URL=https://tudominio.com

# Default language locale (e.g. en)
NEXT_PUBLIC_APP_LOCALE=en

# ============================================================
# OPTIONAL — only needed if you use the feature.
# ============================================================

# Defense-in-depth allow-list for the hostnames that
# /api/account/invitations is willing to publish in invite URLs.
# Comma-separated, no scheme, no port (just hostnames).
#
# Why this exists: when NEXT_PUBLIC_SITE_URL is unset, invite URLs
# are derived from the incoming request's `Host` / `X-Forwarded-
# Host` header. On a typical proxied deploy the proxy sets these
# to your canonical hostname and they're trustworthy. On a *bare*
# deployment exposed to the public internet, an attacker could
# POST to the API directly with a spoofed `Host: phishing.example`
# and receive an invite URL pointing at their site.
#
# When this var is set, hostnames not on the list are rejected
# (the request falls through to the wacrm.tech fallback with a
# console.warn). When unset, behavior is unchanged from earlier
# versions — the request-derived host is trusted.
#
# Most operators don't need this: setting NEXT_PUBLIC_SITE_URL to
# your canonical URL already pins invite links there. This is for
# operators who want belt-and-braces or run multi-tenant setups
# where one app instance serves several hostnames.
#
# Example:
#   ALLOWED_INVITE_HOSTS=crm.example.com,crm-staging.example.com

# Shared secret protecting GET /api/automations/cron. Required if you
# use Wait steps in automations (a scheduled pinger drains pending
# executions). The same secret also protects GET /api/automations/queue,
# which drains the anti-spam message_queue (DAD §8.3 P1.4), GET
# /api/flows/cron (waits de flows) and GET /api/conversions/cron
# (reporting de conversiones a Google/Meta). If you use frequency_rules,
# flows o el conversion loop, add those routes to the same schedule.
# Generate any long random string:
#   openssl rand -hex 32
# See docs/docker.md (tabla de endpoints y frecuencias) y
# docs/automations-and-cron.md.
# AUTOMATION_CRON_SECRET=generate-a-long-random-string

# Telnyx webhook public key (base64, Ed25519). Verifies the signature on
# every inbound Telnyx webhook POST (src/lib/telnyx/webhook-signature.ts).
# The webhook FAILS CLOSED without it — inbound calls/SMS are rejected
# with 401 until this is set. Get it from Telnyx Dashboard → API Keys →
# Public Key (the base64-encoded Ed25519 public key).
# TELNYX_WEBHOOK_PUBLIC_KEY=your-telnyx-base64-public-key

# Resend webhook signing secret (Svix). Verifies every inbound Resend
# webhook POST on /api/email/webhook (src/lib/email/send.ts). The
# endpoint FAILS CLOSED without it — email.delivered/opened/clicked
# events are rejected with 503 until this is set. Get it from Resend
# Dashboard → Webhooks → signing secret.
# RESEND_WEBHOOK_SECRET=whsec_your-resend-signing-secret

# Meta Conversions API (DAD §8.3 Item 16) — CTWA lead reporting.
# datasetId: the Meta Pixel/Dataset ID; accessToken: token with
# conversion reporting permission. Without them dispatchConversion is a
# no-op (fail-open). See src/lib/analytics/meta-capi.ts.
# Also used by the conversion loop (src/lib/conversions/deliver.ts) to
# report website conversion events (Lead, QualifiedLead, Appointment*,
# Purchase) to the CAPI.
# META_CAPI_DATASET_ID=your-meta-pixel-id
# META_CAPI_ACCESS_TOKEN=your-meta-capi-access-token

# MVP Meta CAPI: código de la pestaña "Eventos de prueba" de Events
# Manager, para verificar los eventos que envía el CRM (DEF-4). Solo para
# verificación manual; déjalo vacío en producción.
# META_CAPI_TEST_EVENT_CODE=TEST12345

# Geolocalización por IP (src/lib/analytics/ip-geo.ts) — OPCIONAL.
# IPGEO_URL es una plantilla donde {ip} se sustituye por la IP del
# visitante; la respuesta esperada es JSON {city, region, postal, country}.
# IPGEO_KEY viaja como Bearer si está definida. Sin IPGEO_URL el módulo no
# llama a nadie (devuelve {}). Verifica los términos y límites del
# proveedor que elijas antes de fijarlo — varios servicios gratuitos son
# solo HTTP o prohíben el uso comercial. Las cabeceras de plataforma
# (x-vercel-ip-*, cf-ip*) se usan primero y no necesitan nada aquí.
# IPGEO_URL=https://api.ejemplo.com/geo?ip={ip}
# IPGEO_KEY=your-ipgeo-key

# Google Ads — Data Manager API (conversion loop, option A approved).
# Since June 15, 2026 UploadClickConversions is deprecated for offline
# conversion imports; the adapter src/lib/analytics/google-ads.ts uses
# the Data Manager API (POST /v1/events:ingest, OAuth scope
# https://www.googleapis.com/auth/datamanager).
#   customerId:         Google Ads customer ID, no hyphens (1234567890)
#   conversionActionId: the conversion action ID with source
#                       "Website (Import from clicks)" (UPLOAD_CLICKS)
#   oauthToken:         OAuth2 access token with the datamanager scope
# Without them the cron skips Google deliveries (fail-open).
# GOOGLE_ADS_CUSTOMER_ID=1234567890
# GOOGLE_ADS_CONVERSION_ACTION_ID=987654321
# GOOGLE_ADS_OAUTH_TOKEN=ya29-your-oauth-token

# The conversion loop cron (GET /api/conversions/cron) uses the SAME
# shared secret as AUTOMATION_CRON_SECRET via the x-cron-secret header.
# Add /api/conversions/cron to the same schedule that drains
# automation_pending_executions.

# Account that owns the site's tracking events (sitio web → CRM).
# Resolves the account_id for the anonymous /api/events and /api/track
# endpoints (src/lib/analytics/landing-account.ts). If unset, the
# resolver falls back to the FIRST account created (order by created_at).
# Set it when you deploy so form_submit leads land in the correct account
# regardless of which account was created first.
# LANDING_ACCOUNT_ID=your-account-uuid

# Meta App ID (Meta for Developers → App Settings → Basic). Required to
# create/edit message templates with an IMAGE header: Meta only accepts a
# Resumable-Upload media handle (not a plain URL) as the header sample, and
# that upload is app-scoped. Without it, image-header template submission
# returns a clear error; text/body-only templates are unaffected. Pair
# with META_APP_SECRET.
# META_APP_ID=your-meta-app-id

# When "true", POST /api/whatsapp/templates/submit skips the Meta call
# and stores the row with a synthetic `dry-run-<uuid>` meta_template_id.
# Set this in CI and local development so you can exercise the full
# template UI without a real WABA. Leave unset (or "false") in prod.
# WHATSAPP_TEMPLATES_DRY_RUN=true

# ------------------------------------------------------------------
# AI reply assistant (optional)
# ------------------------------------------------------------------
# The AI assistant is bring-your-own-key: each account pastes its own
# OpenAI or Anthropic key under Settings → AI Assistant. The key is
# stored AES-256-GCM-encrypted with ENCRYPTION_KEY (above) — there is
# NO global provider key env var, and nothing here is required for the
# feature to work. The two vars below only tune behaviour.
#
# The AI knowledge base (migration 030) uses Postgres full-text search
# out of the box. Optional semantic search needs the `pgvector`
# extension — migration 030 runs `CREATE EXTENSION IF NOT EXISTS vector`
# (Supabase has it available) — plus a per-account embeddings key set in
# Settings → AI Assistant. Still no env var required.

# Per-call timeout for provider requests, in milliseconds. Default 30000.
# AI_REQUEST_TIMEOUT_MS=30000

# How many recent text messages of a conversation to send the model as
# context (draft + auto-reply). Default 20.
# AI_CONTEXT_MESSAGE_LIMIT=20

# ------------------------------------------------------------------
# Twilio / SendGrid (opcional — modelo BYO, credenciales por cuenta)
# ------------------------------------------------------------------
# No hay credenciales globales de Twilio ni de SendGrid: cada cuenta pega
# las suyas en Settings y se guardan encriptadas con ENCRYPTION_KEY en
# `twilio_config` / `sendgrid_config`. Si alguien propone un
# TWILIO_ACCOUNT_SID global, ha entendido mal el modelo de tenancy.
#
# Lo único que SÍ es global es la base pública desde la que se construyen
# las URLs de webhook. Twilio calcula X-Twilio-Signature sobre la URL
# COMPLETA, así que este valor tiene que coincidir byte a byte con lo que
# esté configurado en la consola de Twilio: un http en vez de https, o una
# barra final de más, invalidan TODAS las peticiones con un 403 y sin más
# pista que ese 403.
#
# No se deriva de la petición a propósito: un proxy que reescriba el host
# (o un x-forwarded-proto que se pierda) rompería la firma en silencio.
# Sin barra final. Si no está, se usa NEXT_PUBLIC_SITE_URL.
# TWILIO_WEBHOOK_BASE_URL=https://crm.tudominio.com
